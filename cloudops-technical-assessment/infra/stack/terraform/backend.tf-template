terraform {
  backend "s3" {
    bucket       = "my-tf-state-bucket"
    key          = "cloudops/stack/terraform.tfstate"
    region       = "eu-west-1"
    encrypt      = true
    use_lockfile = true
  }
}
